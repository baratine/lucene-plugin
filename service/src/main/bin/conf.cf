server_port = 8085;

cluster {
  server port=$server_port {
    #jvm-arg "-agentlib:hprof=cpu=times,interval=1,depth=10";
  }
  #logger finest;
  #logger finer "examples";
  #logger off "com.caucho.amp.proxy";
}
